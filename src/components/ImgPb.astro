---
const { record, field = "photo", alt, classPicture, classImg, thumb = "1024x1024" } = Astro.props;

const pb = Astro.locals.pb;
const imageUrl = record?.[field] ? pb.files.getURL(record, record[field], { thumb }) : null;
---

{imageUrl ? (
  <div class={classPicture}>
    <img class={classImg} alt={alt || "Image"} src={imageUrl}/>
  </div>
) : (
  <div class={classPicture + " w-full h-full lg:h-96 flex items-center justify-center"}>
    <span class="text-white">Image non disponible</span>
  </div>
)}